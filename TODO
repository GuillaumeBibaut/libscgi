* scgi_free: something to free all the mess around
* Headers :
  * 'Location' + 'Status' : scgi_redirect(url, permanent)
  * 'Pragma', for cache control
  * 'Expires', also for cache
  * 'Set-cookie', for cookies (really...)

